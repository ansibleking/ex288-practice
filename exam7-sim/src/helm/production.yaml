image:
  repository: image-registry.openshift-image-registry.svc:5000/helm-production/catalog-app
  tag: latest

labels:
  stage: production

resources:
  requests:
    cpu: 250m
    memory: 1Gi
  limits:
    cpu: 250m
    memory: 1Gi

route:
  tls:
    enabled: true
